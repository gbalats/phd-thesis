\definecolor{llvmcodebg}{rgb}{0.95,0.95,0.95}

\newminted[bitcode]{llvm}{%
  gobble=0,
  tabsize=2,
  fontsize=\footnotesize,
  % frame=lines, framesep=5\fboxrule, framerule=1pt,
  % bgcolor=llvmcodebg, rulecolor=\color{gray!40}
}

\newminted[cppcode]{c++}{%
  mathescape,
  gobble=0,
  tabsize=2,
  fontsize=\footnotesize
}

\newminted[lcppcode]{c++}{%
  linenos,
  mathescape,
  gobble=0,
  tabsize=2,
  fontsize=\footnotesize
}

\newminted[javacode]{java}{%
  mathescape,
  autogobble,
  tabsize=4,
  fontsize=\footnotesize
}

\newminted[javacodelinum]{java}{%
  mathescape,
  autogobble,
  tabsize=4,
  fontsize=\footnotesize,
  linenos
}

\newminted[bytecode]{java}{%
  mathescape,
  autogobble,
  tabsize=2,
  fontsize=\footnotesize
}


% Custom Datalog

\newcommand{\args}[1]{\(#1\)}
\newcommand{\predname}[1]{\textsc{\textbf{\small{#1}}}}
\newcommand{\pred}[2]{\predname{#1}(\args{#2})}
\newcommand{\funcpred}[3]{\predname{#1} (\args{#2}) = \args{\textbf{#3}}}
\newcommand{\rulearrow}{{\large \textbf{$\leftarrow$}}}
\newcommand{\tab}{\hspace{0.3cm}}
\newcommand{\rulesep}{\vspace{2mm}}

\newenvironment{rules}{%
\vspace{1mm}%
  \hspace{0mm}          % Add vertical and horizontal space
  \begin{minipage}{\columnwidth} % begin minipage
    \hrule\vspace{2mm}               % add beginning horizontal rule
    \begin{tabular}{l}               % begin tabular
}{
    \end{tabular}               % end tabular
  \vspace{2mm}\hrule            % add ending horizontal rule
  \end{minipage}                % end minipage
\vspace{1mm}                    % add vertical space
}
